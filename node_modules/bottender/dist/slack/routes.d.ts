import { Action, Client, Event } from '../types';
import { RoutePredicate } from '../router';
import { EventTypes } from './SlackEvent';
declare type Route = <C extends Client = any, E extends Event = any>(action: Action<C, E>) => {
    predicate: RoutePredicate<C, E>;
    action: Action<C, E>;
};
declare type Slack = Route & {
    message: Route;
    event: <C extends Client = any, E extends Event = any>(eventType: EventTypes, action: Action<C, E>) => {
        predicate: RoutePredicate<C, E>;
        action: Action<C, E>;
    };
};
declare const slack: Slack;
export default slack;
//# sourceMappingURL=routes.d.ts.map