import { SlackOAuthClient } from 'messaging-api-slack';
import Bot from '../bot/Bot';
import SessionStore from '../session/SessionStore';
import { SlackRequestBody } from './SlackConnector';
import SlackEvent from './SlackEvent';
export default class SlackBot extends Bot<SlackRequestBody, SlackOAuthClient, SlackEvent> {
    _accessToken: string;
    constructor({ accessToken, sessionStore, sync, verificationToken, origin, skipLegacyProfile, }: {
        accessToken: string;
        sessionStore?: SessionStore;
        sync?: boolean;
        verificationToken?: string;
        origin?: string;
        skipLegacyProfile?: boolean | null;
    });
    createRtmRuntime(): void;
}
//# sourceMappingURL=SlackBot.d.ts.map